var assert = require("assert")

describe("Some addition test", () => {
  it("should return true", () => {
    assert.strictEqual(1 + 1, 2)
  })
})

describe("Some addition test", () => {
  it("should return true", () => {
    assert.strictEqual(1 + 3, 2)
  })
})
